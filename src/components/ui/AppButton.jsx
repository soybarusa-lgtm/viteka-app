// @deprecated — no se usa en el proyecto. Pendiente de eliminar.
// El proyecto usa las clases CSS de index.css (.btn-primary, .btn-secondary) directamente.
export default function AppButton() { return null }
